@model Material
@{
    ViewData["Title"] = "Добавить материал";
}

<div class="row justify-content-center">
<div class="col-md-6">
<div class="card shadow-sm">
    <div class="card-header bg-primary text-white">
        <h5 class="mb-0"><i class="bi bi-plus-circle me-2"></i>Добавить материал</h5>
    </div>
    <div class="card-body">
        <form asp-action="Create" method="post">
            @Html.AntiForgeryToken()
            <div class="mb-3">
                <label asp-for="Name" class="form-label fw-semibold"></label>
                <input asp-for="Name" class="form-control" placeholder="Например: Сталь листовая" />
                <span asp-validation-for="Name" class="text-danger small"></span>
            </div>
            <div class="mb-3">
                <label asp-for="Quantity" class="form-label fw-semibold"></label>
                <input asp-for="Quantity" class="form-control" type="number" step="0.01" min="0" />
            </div>
            <div class="mb-3">
                <label asp-for="UnitOfMeasure" class="form-label fw-semibold"></label>
                <select asp-for="UnitOfMeasure" class="form-select">
                    <option value="кг">кг</option>
                    <option value="шт">шт</option>
                    <option value="литр">литр</option>
                    <option value="м">м</option>
                    <option value="м²">м²</option>
                    <option value="упак">упак</option>
                </select>
            </div>
            <div class="mb-4">
                <label asp-for="MinimalStock" class="form-label fw-semibold"></label>
                <input asp-for="MinimalStock" class="form-control" type="number" step="0.01" min="0" />
                <div class="form-text">При падении ниже этого значения — предупреждение</div>
            </div>
            <div class="d-flex gap-2">
                <button type="submit" class="btn btn-primary flex-fill"><i class="bi bi-save me-1"></i>Сохранить</button>
                <a href="/Materials" class="btn btn-outline-secondary">Отмена</a>
            </div>
        </form>
    </div>
</div>
</div>
</div>
